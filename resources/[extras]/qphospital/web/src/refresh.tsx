import { refresh } from "./action";

export refresh {
  sethealthArmour: true
}

  const healthArmour = healthArmour();

  const healthArmour = (
    refresh: data
  ) => {
    healthArmour.value[refresh] = refresh;
    refreshHealthArmour(healthArmour[refresh]);
  };

  syncData('fullhealthArmour', (data: healthArmour[]) => {
    healthArmour(data);
  });

  return (
      healthArmour.value(healthArmour, refresh) => (
  );

export refresh healthArmour;
